echo "🧪 Running all tests with coverage before push..."

pnpm test

if [ $? -ne 0 ]; then
  echo "❌ Tests failed or coverage requirements not met. Push aborted."
  exit 1
fi

echo "✅ All tests passed and coverage requirements met. Proceeding with push..."